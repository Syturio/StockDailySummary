import { AzureEndpoint } from '../azure-arm-rest/azureModels';
export declare class AzureResourceFilterUtility {
    static getAppDetails(endpoint: AzureEndpoint, resourceName: string): Promise<any>;
}
